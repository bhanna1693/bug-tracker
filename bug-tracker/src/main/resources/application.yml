spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/bug_tracker?serverTimezone=UTC
    username: root
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#  lifecycle:
#    timeout-per-shutdown-phase: 30s
#  security:
#    user:
#      name: user
#      password: password
#
#server:
#  shutdown: graceful

